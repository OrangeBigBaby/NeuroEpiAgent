# constraints.txt — auditable dependency snapshot for NeuroSurgEpiAgent.
#
# Purpose
# -------
# This file is an *auditable reproducibility snapshot* of one verified-good
# dependency set, NOT a hard runtime requirement. The package's real runtime
# floors are the `>=` bounds declared in `pyproject.toml`
# (pydantic>=2.5, pyyaml>=6.0; dev/test extra adds pytest>=7, pandas>=2.0).
#
# CI (`.github/workflows/tests.yml`) installs the LATEST compatible releases
# (`pip install -e ".[dev]"`, no `-c`) on every push so drift is caught. This
# file is the complementary anchor: it records one concrete set of versions the
# suite passed against, so a reviewer or archivist can reproduce that exact
# environment:
#
#     python -m pip install -e ".[dev]" -c constraints.txt
#
# Update policy
# -------------
# Bump this snapshot only after the full suite passes locally AND the clean
# clone + CI matrix pass against the new versions. Do not tighten the
# `pyproject.toml` floors to match this file without a documented reason
# (e.g. an incompatible upstream change); the floors stay loose on purpose so
# the library remains installable in downstream environments.
#
# Verified snapshot (CPython 3.12, Windows + Ubuntu) at v0.3.1:
pydantic==2.10.3
pyyaml==6.0.2
pytest==9.1.1
pandas==2.3.3
numpy==2.3.5
setuptools==75.8.0
wheel==0.45.1
